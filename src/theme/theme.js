import {
  text,
  breakpoints,
  layout,
  fonts,
  lineHeights,
  colors,
  radii,
  buttons,
  links,
} from "./config";

export const theme = {
  breakpoints,
  layout,
  fonts,
  lineHeights,
  text,
  colors,
  radii,
  buttons,
  links,
};
